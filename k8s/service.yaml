apiVersion: v1
kind: Service
metadata:
  labels:
    app: peternetes-web
  name: peternetes-web
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: peternetes-web
  type: LoadBalancer
